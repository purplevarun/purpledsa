#!/usr/bin/env bash
set -euo pipefail

PROJECT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$PROJECT_DIR"

case "$1" in
  dev)
    shift
    npm run dev -- "$@"
    ;;
  format)
    shift
    npm run format -- "$@"
    ;;
  *)
    echo "Usage: ./run dev|format" >&2
    exit 1
    ;;
esac
